define host {
    use                     avv-linux-server          ; Inherit default values from a template
    host_name               ns
    alias                   DNS primary server
    address                 ns.home.lan
    hostgroups              dns-servers
}

define host {
    use                     avv-linux-server          ; Inherit default values from a template
    host_name               ns2
    alias                   DNS 1 secondary server
    address                 ns2.home.lan
    hostgroups              dns-servers
}

define host {
    use                     avv-linux-server          ; Inherit default values from a template
    host_name               ns3
    alias                   DNS 2 secondary server
    address                 ns3.home.lan
    hostgroups              dns-servers
}

define host {
    use                     avv-linux-server          ; Inherit default values from a template
    host_name               nsr
    alias                   DNS main server
    address                 nsr.home.lan
    hostgroups              dns-servers
}

